{
  "name": "tunnel-e2e-test",
  "version": "1.0.0",
  "description": "Playwright test to verify tunnel proxy support in Relicx builds",
  "scripts": {
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  }
}

